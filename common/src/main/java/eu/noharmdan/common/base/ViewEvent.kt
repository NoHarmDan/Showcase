package eu.noharmdan.common.base

import eu.noharmdan.common.util.getOnEvent

/**
 * Interface used for one-time events from a compose UI to its subclass of [BaseViewModel].
 *
 * @see getOnEvent
 */
interface ViewEvent